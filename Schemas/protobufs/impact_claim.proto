/* Conveys the details of an impact claim made by an agent. */

syntax = "proto3";

message ImpactClaim {
  string identifier = 1;
  Agent claimant = 2;
  State state_before = 3;
  State state_after = 4;
  repeated Activity activities = 5;
}