syntax = "proto3";

package base;

message Variable {
 string symbol = 1; // The symbol that represents this variable in the formula field of the associated calculation class instance.
 string id_parval = 2;
}

message Calculation {
 string formula = 1;
 repeated Variable values = 2;
}