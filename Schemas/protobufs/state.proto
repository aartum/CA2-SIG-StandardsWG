
syntax = "proto3";

message State {
  string identifier = 1;
  Environment environment = 2; // The environment to which the state belongs.
  DateTime date_time = 3; // The moment of measurement
  repeated ParVal par_vals = 4;
}

